# afk-spec Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an `afk-spec` skill that carries a full written spec to a single open PR unattended, by wrapping `writing-plans` and `subagent-driven-development` and adding a fixed PR tail.

**Architecture:** A thin orchestration `SKILL.md`. It invokes `superpowers:writing-plans` (spec -> ordered plan), then `superpowers:subagent-driven-development` (the whole execution loop - worktree, sequential implementers, maker/checker reviews, ledger), then its own tail that always opens exactly one non-draft PR. It reimplements none of the loop. It differs from the sibling `afk-issues` skill in one deliberate way: its two superpowers skills are **hard** dependencies (the wrapper is nothing without them), not the optional power-ups `afk-issues` treats them as.

**Tech Stack:** Markdown skill files, JSON plugin manifests. No runtime code. Validation is structural (frontmatter parses, JSON well-formed, required content present) plus a read-through, matching this repo's stated convention.

## Global Constraints

- British English throughout; conventional commit prefixes (`feat:`, `docs:`).
- Opening exactly one **non-draft** PR is the mandatory terminal outcome of every run. The run **never** calls `gh pr ready` and never ends without a PR.
- CI checks are out of scope: the tail does not watch, wait on, or gate on them.
- The wrapper does **not** reimplement the execution loop and does **not** invoke `superpowers:finishing-a-development-branch`.
- The two superpowers skills (`writing-plans`, `subagent-driven-development`) are hard dependencies: the skill checks they are present at start and fails fast with a clear message if either is absent - it does not degrade.
- Skills are auto-discovered from `skills/<name>/SKILL.md`; the new skill is `afk-spec:afk-spec`.
- `plugin.json` and `marketplace.json` versions are bumped together (repo convention).
- Sequential task execution only (inherited from SDD); no parallel implementers.

---

### Task 1: Write the afk-spec skill

**Files:**
- Create: `skills/afk-spec/SKILL.md`

**Interfaces:**
- Consumes: `superpowers:writing-plans` and `superpowers:subagent-driven-development` (invoked as skills in-session); `gh` on PATH, authenticated, cwd is the target repo.
- Produces: the skill `afk-spec:afk-spec`, discoverable once the plugin is installed. No exported code symbols.

- [ ] **Step 1: Create the skill file with valid frontmatter and the full body**

Create `skills/afk-spec/SKILL.md` with exactly this content:

````markdown
---
name: afk-spec
description: Use when the user wants to carry a full written spec to a single open pull request unattended - plan it, build it across sequential subagent tasks on one branch, review the whole branch, then open one PR. e.g. "/afk-spec docs/specs/thing.md", "afk-spec this design doc", "work this spec to a PR while I'm away".
---

# AFK Spec

Carry one written spec to one open pull request, unattended. You are the
**orchestrator**. You do not plan, implement, or review with your own eyes -
you invoke the skills that do, in order, and then open the PR. The loop itself
is `superpowers:subagent-driven-development`; this skill is the spec front door
and the PR tail around it, nothing more.

Assumes the current directory is the target repo and `gh` is authenticated.

## Hard dependencies (check first, fail fast)

Unlike the sibling `afk-issues` skill, whose superpowers helpers are optional
power-ups, this skill is **nothing without its two dependencies**. Before doing
any work, confirm both are in your available skills list:

- `superpowers:writing-plans`
- `superpowers:subagent-driven-development`

If either is missing, **stop** and tell the user once, plainly: this skill wraps
those two skills and cannot run without them; install the superpowers plugin
(`/plugin marketplace add obra/superpowers` then
`/plugin install superpowers@superpowers-dev`) and re-run. Do not attempt a
degraded run - there is nothing to degrade to.

## The one rule

**Every run ends with exactly one open, non-draft PR.** Even when the final
whole-branch review still has open findings, you open the PR and summarise those
findings honestly in its body. You never call `gh pr ready` (creating non-draft
once is what routes around the self-approval classifier). You never merge - the
human does that.

## Steps

### 1. Resolve the spec

Take the spec path from the invocation (`/afk-spec <path>`). Read it. If no path
was given and the request is genuinely ambiguous, ask once; otherwise go
straight to work.

### 2. Plan

Invoke `superpowers:writing-plans` on the spec to produce a dependency-**ordered**
task plan with per-task acceptance criteria. The plan's order is the dependency
graph flattened - a task that depends on another simply comes later on the one
branch, so there is no blocker/waiting handling to do here.

### 3. Execute

Invoke `superpowers:subagent-driven-development` to run the plan. That skill owns
the entire loop: it isolates one worktree, dispatches a fresh implementer per
task sequentially, runs a task review after each, runs a broad whole-branch
review at the end, and tracks progress in its on-disk ledger. Do not duplicate
or second-guess any of it. Hand it two operating overrides for unattended use:

- **Subagents log, don't ask.** When you compose an implementer dispatch prompt,
  instruct the subagent to make the reasonable assumption and record it in its
  report rather than asking a question - a mid-run question stalls an unattended
  run. (Same discipline `afk-issues` workers use.)
- **Halt only on a genuine plan contradiction.** SDD's pre-flight plan review and
  its plan-contradiction escalations stay valid halt points - a real
  contradiction should stop the run, not be guessed past. Everything short of a
  contradiction proceeds and is logged, not raised to the human mid-run.

### 4. Open the PR (fixed tail)

Do **not** invoke `superpowers:finishing-a-development-branch`. Its menu is
interactive; this tail is fixed. Once SDD's final whole-branch review has run:

Run, once:

```bash
gh pr create --title "<spec title>" --body "<body>"
```

- Non-draft (omit `--draft`). Never follow with `gh pr ready`.
- The body lists what the branch delivers against the spec's acceptance criteria,
  and summarises any outstanding findings from the final review.

Then report the PR URL to the user and stop. Do not watch CI, do not merge.

## Common Mistakes

When you catch yourself thinking the excuse, the reality is the rule.

| Excuse | Reality |
|--------|---------|
| "I'll just plan and build it myself, it's a small spec" | You orchestrate. `writing-plans` plans, SDD builds and reviews. Invoke them. |
| "SDD wants to check in before Task 1, I'll relay that to the user" | Only a genuine plan contradiction halts. Pre-flight noise is logged and passed through, not raised mid-run. |
| "The implementer asked a question, I'll wait for the human" | Unattended. Tell implementers to log the assumption and proceed. A question that reaches the human mid-run has already stalled the loop. |
| "I'll open it draft, then ready it once the review passes" | Never. Draft-then-ready is the self-approval flip the classifier blocks. Open non-draft, once. |
| "The final review found issues, so I won't open a PR" | Open the PR anyway and summarise the findings in the body. A PR is the mandatory outcome - the human triages from there. |
| "Let me watch CI before I finish" | Out of scope. Open the PR, report the URL, stop. |
| "I'll call finishing-a-development-branch to wrap up" | No - its menu is interactive. Run the fixed tail: create non-draft, report, stop. |
| "superpowers isn't installed, I'll do a best-effort version" | Hard dependency. Stop and tell the user to install it. There is nothing to degrade to. |
````

- [ ] **Step 2: Verify the frontmatter parses and required content is present**

Run:

```bash
cd /Users/barrydobson/_git/barrydobson/afk-issues-skill
head -4 skills/afk-spec/SKILL.md
grep -c "gh pr ready" skills/afk-spec/SKILL.md
grep -q "superpowers:writing-plans" skills/afk-spec/SKILL.md && grep -q "superpowers:subagent-driven-development" skills/afk-spec/SKILL.md && echo "deps referenced"
grep -q "finishing-a-development-branch" skills/afk-spec/SKILL.md && echo "finishing referenced (should be in a 'do NOT invoke' context)"
```

Expected: frontmatter shows `name: afk-spec` and a `description:` line; `gh pr ready` appears (only inside "never call" prohibitions - eyeball it); both deps referenced; the finishing-a-development-branch reference is the "do not invoke" one.

- [ ] **Step 3: Read the file through once**

Confirm: the hard-dependency check fails fast, the PR-always rule is stated, both overrides are present, the tail creates non-draft and never readies, CI is explicitly out of scope. Fix any drift inline.

- [ ] **Step 4: Commit**

```bash
cd /Users/barrydobson/_git/barrydobson/afk-issues-skill
git add skills/afk-spec/SKILL.md
git commit -m "feat: add afk-spec skill (spec to one open PR, unattended)"
```

---

### Task 2: Register and document the skill

**Files:**
- Modify: `.claude-plugin/plugin.json` (version bump)
- Modify: `.claude-plugin/marketplace.json` (version bump, matching)
- Modify: `README.md` (add afk-spec to the inventory and usage)
- Modify: `CLAUDE.md` (add to the file map; note the hard-dependency posture difference)

**Interfaces:**
- Consumes: the skill file from Task 1.
- Produces: an installable plugin whose manifests list the new version, and docs that describe `afk-spec` alongside `afk-issues`.

- [ ] **Step 1: Bump both manifest versions from 0.9.1 to 0.10.0**

In `.claude-plugin/plugin.json`, change `"version": "0.9.1"` to `"version": "0.10.0"`.
In `.claude-plugin/marketplace.json`, change the plugin entry's `"version": "0.9.1"` to `"version": "0.10.0"`.

(Minor bump: a new skill is an added capability, backward compatible.)

- [ ] **Step 2: Verify both JSON files are well-formed and versions match**

Run:

```bash
cd /Users/barrydobson/_git/barrydobson/afk-issues-skill
python3 -c "import json; a=json.load(open('.claude-plugin/plugin.json'))['version']; b=json.load(open('.claude-plugin/marketplace.json'))['plugins'][0]['version']; print(a,b); assert a==b=='0.10.0', 'version mismatch'"
```

Expected: prints `0.10.0 0.10.0`, no assertion error.

- [ ] **Step 3: Add afk-spec to README.md**

In the "What's inside" table, add a row after the `pr-reviewer` row:

```markdown
| `skills/afk-spec/SKILL.md` | A second orchestrator: carries one written **spec** to one open PR unattended, by wrapping superpowers' `writing-plans` and `subagent-driven-development`. Where `afk-issues` clears a backlog of tickets, `afk-spec` drives a single spec to a single branch and PR. |
```

Then add a short usage note after the existing Usage block:

```markdown
### afk-spec: a whole spec to one PR

Where `afk-issues` works a backlog of independent tickets, `afk-spec` takes one
detailed spec and drives it to a single open PR unattended - it plans the spec
into ordered tasks, builds them sequentially on one branch (dependencies become
order, not merge gates), reviews the whole branch, and opens exactly one PR.

```
/afk-spec docs/specs/my-feature.md
afk-spec this design doc while I'm away
```

`afk-spec` **requires** the superpowers plugin (it wraps two of its skills) -
unlike the optional power-ups above, it will not run without it.
```

- [ ] **Step 4: Add afk-spec to CLAUDE.md**

In the file-map code block, add after the `references/pr-review.md` line:

```
skills/afk-spec/SKILL.md      the second orchestrator (skill `afk-spec:afk-spec`) - one written spec -> one open PR, wrapping superpowers' writing-plans + subagent-driven-development
```

Then add a paragraph to the "Optional dependency posture" section noting the exception:

```markdown
`afk-spec` is the deliberate exception to the optional-dependency posture above:
its two superpowers skills (`writing-plans`, `subagent-driven-development`) are
**hard** dependencies - it wraps them and has nothing to fall back to, so it
fails fast if they are absent rather than degrading.
```

- [ ] **Step 5: Verify the docs reference the new skill**

Run:

```bash
cd /Users/barrydobson/_git/barrydobson/afk-issues-skill
grep -q "afk-spec" README.md && grep -q "afk-spec" CLAUDE.md && echo "docs updated"
```

Expected: prints `docs updated`.

- [ ] **Step 6: Commit**

```bash
cd /Users/barrydobson/_git/barrydobson/afk-issues-skill
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md CLAUDE.md
git commit -m "docs: register afk-spec skill and bump to 0.10.0"
```

---

## Validation (real end-to-end test)

Structural checks prove the files are well-formed; they do not prove the skill
*works*. The genuine test is one live run, best done by the human (it spawns the
full SDD loop and real subagents - not something to nest inside a build
subagent). Do this once after Task 2, in a throwaway repo or a scratch branch:

1. Write a tiny spec with two tasks where the second depends on the first (e.g. "add a `greet(name)` function", then "add a CLI that calls `greet`").
2. Install the updated plugin, run `/afk-spec <that-spec>`.
3. Confirm the terminal state: one branch containing both tasks' work, one **non-draft** PR open, and no `gh pr ready` call anywhere in the run. The dependent task must have been built on top of the first on the same branch - never parked as "waiting".

If the run stalls asking a question mid-way, the override wording in Task 1
Step 1 needs strengthening - fix the SKILL.md and re-run.

## Self-Review

- **Spec coverage:** plan front door (Task 1 §2), SDD execution untouched (§3), fixed PR tail always-non-draft (§4 + Global Constraints), unattended overrides (§3), hard-dependency fail-fast (Task 1 hard-deps section), no `finishing-a-development-branch`, CI out of scope, `afk-issues` untouched - all covered.
- **Placeholder scan:** the SKILL.md body is complete verbatim; the only angle-bracket placeholders (`<path>`, `<spec title>`, `<body>`) are runtime values the orchestrator fills, not plan gaps.
- **Type consistency:** no code symbols; skill/skill-name references (`afk-spec:afk-spec`, the two superpowers skill names) are consistent across both tasks and the manifests.
